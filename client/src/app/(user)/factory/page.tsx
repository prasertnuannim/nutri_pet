"use client";

import { Factory } from "lucide-react";
import SectionPlaceholder from "../section-placeholder";

export default function FactoryPage() {
  return (
    <SectionPlaceholder
      title="userPlaceholder.factoryTitle"
      description="userPlaceholder.factoryDescription"
      icon={Factory}
    />
  );
}
